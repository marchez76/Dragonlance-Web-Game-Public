{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dragonlance-gdr.local/schema/vocabolari.schema.json",
  "title": "Vocabolari condivisi — la sede unica dei termini che due schemi confrontano",

  "description": "PERCHE' ESISTE. Un termine che compare in DUE schemi e che un motore deve confrontare non e' una convenzione: e' una struttura doppia in attesa di sfasarsi, e questo progetto ne ha gia' viste sette. L'ottava e' stata trovata USANDO i dati e non ispezionandoli — dati/oggetti/ portava i tipi di danno in inglese (`slashing`, copiati dall'SRD) e dati/mostri/ in italiano (`perforante`), nessuno dei due schemi li vincolava e nessuno dei due era sbagliato dal proprio lato. Un motore che confronti un tipo di danno con una resistenza li mancava tutti. Qui il vocabolario e' UNO, sta in UN file, e gli schemi che lo usano lo RIFERISCONO con `$ref` invece di ricopiarlo: ricopiare un enum in tre schemi sarebbe una struttura doppia nuova, creata mentre si chiude l'ottava. Il codice Python non lo ridigita: `dati/_vocabolari.py` legge questo file. LINGUA: italiano, come ogni altro enum che il progetto possiede nello strato `mechanics_5e` (`mischia_arma`, `da_guerra`, `leggera`, `riposo_breve`, gli id di dati/condizioni/). L'inglese in `weapon_5e.damage_type` non era una scelta: era una stringa di fonte che `build_oggetti.py` copiava senza tradurre, cioe' strato di fonte finito nello strato nostro — l'opposto della decisione 7 (`doppio-strato`). L'originale inglese resta dov'e' la fonte, in `dati/_fonti/srd51_equipaggiamento.py`.",

  "$defs": {
    "tipo_danno": {
      "type": "string",
      "enum": [
        "contundente",
        "perforante",
        "tagliente",
        "da_acido",
        "da_freddo",
        "da_fulmine",
        "da_fuoco",
        "da_forza",
        "da_tuono",
        "da_veleno",
        "necrotico",
        "psichico",
        "radiante",
        "nessuno"
      ],
      "description": "I tredici tipi di danno della 5e, piu' `nessuno`. `nessuno` e' un segnaposto NOSTRO, non un tipo della 5e: serve dove la fonte non ne dichiara uno (la Rete dell'SRD, arma di controllo che non infligge danno) e distingue `la fonte non lo dice` da `non e' stato ancora scritto`, la stessa distinzione di `esito.nessuno` in effetto.schema.json."
    },

    "qualificatore_danno": {
      "type": "string",
      "enum": ["da_attacchi_non_magici", "da_armi_non_magiche_non_argentate"],
      "description": "La clausola che restringe una resistenza o un'immunita' a una parte degli attacchi di quel tipo. Esiste perche' senza di essa la clausola resta dentro la stringa — «bludgeoning, piercing, and slashing from nonmagical attacks» era UN elemento di un array di stringhe, cioe' prosa in un campo che sembrava un vocabolario. Enum e non testo libero: e' la differenza fra un dato e una frase, e la frase e' gia' quello che c'era."
    },

    "condizione": {
      "type": "string",
      "enum": [
        "accecato",
        "affascinato",
        "afferrato",
        "assordato",
        "avvelenato",
        "incapacitato",
        "incosciente",
        "invisibile",
        "paralizzato",
        "pietrificato",
        "prono",
        "sfinimento",
        "spaventato",
        "stordito",
        "trattenuto"
      ],
      "description": "Le quindici condizioni dell'Appendice A dell'SRD 5.1, in italiano. L'insieme e' CHIUSO e NOTO: la fonte lo stampa per intero, quindi il vocabolario puo' essere completo anche dove il nostro catalogo non lo e'. QUESTO ENUM NON DICE CHE LA CONDIZIONE E' MODELLATA. Nominare e convertire sono due cose diverse, e tenerle separate e' cio' che permette di chiudere `condition_immunities` senza sconfessare la decisione 48 (`condizioni-a-consumo`): un mostro puo' dichiarare immunita' a `affascinato` — e' un fatto della sua scheda, e perderlo sarebbe perdere informazione vera di fonte — mentre `dati/condizioni/affascinato.json` non esiste ancora, perche' nessun blocco convertito la impone e la cartella si riempie a consumo. Quali siano modellate NON si scrive da nessuna parte: si DERIVA dai file presenti in `dati/condizioni/` (`_vocabolari.condizioni_modellate()`), perche' un elenco scritto a mano accanto a una cartella e' una struttura doppia — la stessa forma che questo file esiste per chiudere. Il motore, quando incontra un'immunita' a una condizione che non sa applicare, la DICHIARA come lacuna: non saperla applicare e non accorgersene sono due cose diverse. NOTA SU `sfinimento`: nella 5e ha sei livelli invece di essere binaria, e quando verra' modellata portera' con se' quella differenza; qui e' un termine come gli altri, perche' un'immunita' lo nomina e basta."
    }
  }
}
